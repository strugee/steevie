package { 'git':
  ensure => latest,
}

package { 'git-doc':
  ensure => latest,
}

package { 'git-svn':
  ensure => latest,
}

package { 'mercurial':
  ensure => latest,
}

package { 'subversion':
  ensure => latest,
}
